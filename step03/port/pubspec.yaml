name: port
description: A web app that uses AngularDart Components
version: 0.0.1

environment:
  sdk: '>=2.0.0-dev.3.0 <2.0.0'

dependencies:
  domain:
    path: ../domain
dev_dependencies:
  test: ^0.12.0